#include <stdio.h>

int main(){
    int s, h, m, frontH, frontM, t1[100], t2[100], i;
    char app[255];
    while(scanf("%d", &s) != EOF){
        for(i = 0; i < s; i++){
            scanf("%d:%d", &h, &m);
        }
    }
    return 0;
}